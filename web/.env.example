# URL base da API Express (usada apenas no servidor / BFF — nunca exposta ao browser)
API_URL=http://localhost:3050

# Segredo para assinar o cookie de sessão (JWT HS256).
# Gere um novo com: openssl rand -base64 32
SESSION_SECRET=troque-este-valor-por-um-segredo-aleatorio
